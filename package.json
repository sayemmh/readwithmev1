{
  "name": "notion-clone-backend",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "engines": {
    "node": "12.x"
  },
  "scripts": {
    "dev": "next dev",
    "build": "cd frontend && next build",
    "start": "cd frontend && start -p $PORT",
    "heroku-postbuild": "cd frontend && npm run build"
  },
  "dependencies": {
    "cheerio": "^1.0.0-rc.5",
    "fetch": "^1.1.0",
    "firebase": "^8.2.7",
    "next": "^10.0.6",
    "request": "^2.88.2"
  }
}
